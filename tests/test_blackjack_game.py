def test_display_funds() -> None:
    """
    Tests that at the start of each game, the user is displayed their funds
    """
    assert False


def test_ask_for_bet() -> None:
    """
    Tests that at the start of each game, the user is asked for the amount they wish to bet
    """
    assert False


def test_display_single_dealer_card() -> None:
    """
    Tests that once the game starts a single card of the dealer is revealed
    """
    assert False


def test_show_user_hand() -> None:
    """
    Tests that once the game starts the users hand is displayed
    """
    assert False


def test_calls_user_action() -> None:
    """
    Tests that the user is asked to act once the game has started
    """
    assert False


def test_calls_dealer_ai_action() -> None:
    """
    Tests that once the user has completed all their actions, the dealer AI is asked to act
    """
    assert False


def test_calls_end_of_game_handle_when_game_is_over() -> None:
    """
    Tests that when the game is over, the handling of the game over is deferred
    """
    assert False


def test_determine_game_over() -> None:
    """
    Tests that the game recognises when its over
    """
    assert False
